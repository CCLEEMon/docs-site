import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebarsDev: SidebarsConfig = {
  devSidebar: [
    'intro',
    'structure',
    'deployment',
    'theming',
    'changelog',
  ],
};

export default sidebarsDev;
